# 安卓手机独立使用指南（不需要电脑）

本项目是零依赖的：服务端可以直接跑在手机上，手机自己当服务器、自己当客户端。
电脑不用开、不用买服务器、不用花一分钱，数据就存在手机里。

## 一、安装 Termux

Termux 是安卓上的终端模拟器，本项目靠它运行 Node.js。

- 下载地址：<https://f-droid.org/packages/com.termux/>（F-Droid 版）
- 或 GitHub Releases：<https://github.com/termux/termux-app/releases>（下载 `termux-app_…_arm64-v8a.apk`）
- **不要用应用商店里的 Termux**——那是废弃的旧版本，装了会在后续步骤出各种怪问题。
- 安装时如果提示「未知来源」，允许即可（Termux 是知名开源软件）。

## 二、把项目传到手机

任选一种（按推荐顺序）：

**方式 A · 电脑打包发过来（最稳）**
在电脑上把整个项目文件夹压缩成 zip（**不要包含 `data/` 文件夹**，那是你电脑上的错题数据），
通过微信「文件传输助手」/ LocalSend / 数据线发到手机，然后在 Termux 里：

```bash
pkg install -y unzip
termux-setup-storage        # 允许 Termux 访问手机存储
unzip ~/storage/downloads/AEN.zip -d ~/
cd ~/AEN                    # 进入解压出来的目录（名字以实际为准）
```

**方式 B · Gitee / GitHub 克隆**
```bash
pkg install -y git
git clone https://gitee.com/你的用户名/Automatic-Error-Notebook.git ~/cuotiji
cd ~/cuotiji
```
（GitHub 在手机网络下可能连不上，Gitee 更稳；先把仓库镜像到 Gitee 再克隆。）

**方式 C · 手机浏览器直接下载 zip**
在手机浏览器打开仓库页面 → Code → Download ZIP，然后同方式 A 解压。

## 三、启动

```bash
bash start-termux.sh
```

首次运行会自动安装 Node.js（几分钟），之后自动启动并打开浏览器。
建议在浏览器菜单里选「**添加到主屏幕**」——桌面会多一个图标，之后点图标就能用。

## 四、配置 AI（只需一次）

打开页面后，右上角「模型设置」里填：

- Base URL（如智谱 `https://open.bigmodel.cn/api/paas/v4`）
- API Key（在你自己的手机上，只有你自己能看到）
- 模型名（如 `glm-4v-plus`，要拍照识题就选带视觉的模型）

也可以直接把电脑上已配好的 `config.json` 随项目一起拷过来，就不用再填了。

> 没有 API Key？页面里切「手动模式」：把生成的提示词粘到任意 AI 聊天工具，
> 再把返回的 JSON 粘回来，功能完全一样。

## 五、日常使用

| 事项 | 说明 |
|---|---|
| 启动 | 点桌面图标，或 Termux 里 `bash start-termux.sh` |
| 停止 | 回到 Termux 按 **音量减键 + C**（即 Ctrl+C），数据自动保存 |
| 被系统杀了 | 安卓后台清理可能杀掉 Termux——重新运行一次脚本即可，**数据不会丢** |
| 备份 | `bash start-termux.sh backup`，错题数据会复制到手机「下载」目录 |
| 升级版本 | 用新文件覆盖旧文件，**保留 `data/` 文件夹**即可（那是你的错题集） |

## 六、注意事项

- **不要卸载 Termux 或在系统设置里「清除数据」**——错题数据（`data/db.json`）存在 Termux 内部存储里，卸载即清空。重要题目定期 `backup`。
- 服务只监听 `127.0.0.1`（脚本已强制）：只有这台手机自己能访问，连同一 Wi-Fi 的别人也进不来。
- 电脑版和手机版数据各自独立。想把电脑上的错题合并到手机：把电脑的 `data/db.json` 一并拷到手机项目目录（会覆盖手机上的数据，先 backup）。

## 七、进阶（可选）：桌面一键启动

安装 F-Droid 的 **Termux:Widget** 插件后：

```bash
mkdir -p ~/.shortcuts
echo '#!/data/data/com.termux/files/usr/bin/bash
bash ~/cuotiji/start-termux.sh' > ~/.shortcuts/错题集.sh
chmod +x ~/.shortcuts/错题集.sh
```

（把 `~/cuotiji` 换成你的实际目录。）桌面添加 Termux Widget 小部件后，
点一下「错题集」即可启动，不用打任何命令。
